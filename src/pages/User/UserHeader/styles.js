import styled from 'styled-components'
import { Container } from '../../../styles/global';
import { LoginTitle } from '../../Login/LoginForm/styles'


export const UserHeaderContainer = styled(Container)``;
export const UserHeaderTitle = styled(LoginTitle)``;