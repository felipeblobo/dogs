import styled from 'styled-components';
import { LoginFormSection, LoginTitle } from '../LoginForm/styles';


export const CreateFormSection = styled(LoginFormSection)``;
export const CreateFormTitle = styled(LoginTitle)``;